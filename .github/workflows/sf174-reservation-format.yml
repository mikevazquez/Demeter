name: SF174 Reservation Format

on:
  push:
    branches:
      - feat/sf-174-asistian-provider

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write supabase/functions/process-booking-created/index.ts tests/sf174-reservation-confirmed-asistian.test.ts
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add supabase/functions/process-booking-created/index.ts tests/sf174-reservation-confirmed-asistian.test.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style(sf174): format reservation Asistian routing"
          git push origin HEAD:feat/sf-174-asistian-provider
