name: Documents panel format helper

on:
  pull_request:
    branches: [main]
    paths:
      - "app/admin/alumnas/[studentId]/StudentDocumentsPanel.tsx"
      - ".github/workflows/documents-panel-format.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write "app/admin/alumnas/[studentId]/StudentDocumentsPanel.tsx"
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "studio-flow-ci"
          git config user.email "studio-flow-ci@users.noreply.github.com"
          git add "app/admin/alumnas/[studentId]/StudentDocumentsPanel.tsx"
          git commit -m "chore(documents): format student documents panel"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
