name: Temporary Prettier Fix NOTIFICACIONES-02 Marketing Detail

on:
  push:
    branches:
      - feat/notificaciones-02-control-center

permissions:
  contents: write

jobs:
  format:
    if: "!contains(github.event.head_commit.message, 'chore: prettier marketing detail')"
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/notificaciones-02-control-center
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write "app/admin/notificaciones/marketing/[marketingKey]/page.tsx"
      - name: Commit formatted file
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed."
            exit 0
          fi
          git config user.name "studio-flow-bot"
          git config user.email "studio-flow-bot@users.noreply.github.com"
          git add "app/admin/notificaciones/marketing/[marketingKey]/page.tsx"
          git commit -m "chore: prettier marketing detail"
          git push origin HEAD:feat/notificaciones-02-control-center
