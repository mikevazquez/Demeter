name: Format 01C Temp

on:
  push:
    branches:
      - feat/notificaciones-01c-delivery-adapters
  workflow_dispatch:

permissions:
  contents: read

jobs:
  format-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx prettier --write supabase/functions/notification-delivery-worker/index.ts
      - name: Show formatting diff
        run: git diff -- supabase/functions/notification-delivery-worker/index.ts
