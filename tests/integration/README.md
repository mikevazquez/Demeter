# Integration test scaffold

This directory is reserved for Studio Flow integration tests that exercise server actions, RLS-sensitive flows, and database-backed domain behavior against an isolated test environment.

The first required scenarios are defined in the Documento Maestro TOTAL v3 and will be implemented phase by phase instead of inventing parallel rules here.
